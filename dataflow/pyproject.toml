[project]
name = "dataflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "msgs",
    "simulation",
    "navigator",
    "policy_controller",
    "tester",
    "teleop",
    "dora-rs-cli>=0.3.13",
    "typer>=0.20.0",
]

[project.scripts]
dataflow = "dataflow.dataflow:main"

[dependency-groups]
dev = [
    "ruff>=0.14.3",
]


